/node_modules
/.next/
/out/
.DS_Store
*.pem
.env*
next-env.d.ts
tsconfig.tsbuildinfo

# The extraction workbench. It holds bml_pool_raw.bin - 567 MB of game data,
# well past GitHub's 100 MB per-file limit - and ~1.3 GB of reference
# screenshots, so the whole folder stays local. The site is built from the
# extracted .htm files in /output, which are committed.
/extract-tool/

# Windows shortcut left in the working directory.
*.lnk
